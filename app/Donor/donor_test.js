'use strict';

describe('AMS.donor module', function() {

  beforeEach(module('AMS.donor'));

  describe('donor controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var donorCtrl = $controller('DonorCtrl');
      expect(donorCtrl).toBeDefined();
    }));

  });
});